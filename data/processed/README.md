# data/processed — generado, no versionado

Salida del ETL (Parte 5) por `<capa>/<version>/`: `<capa>.full.geojson`, `<capa>.web.geojson`, `<capa>.pmtiles` (si supera 5 MB), `reporte_calidad.md`, `reporte_calidad.json`, `metadata.json`.

Se regenera por completo con `pnpm etl:all`. Si algo aquí requiere un paso manual, el pipeline está roto.
